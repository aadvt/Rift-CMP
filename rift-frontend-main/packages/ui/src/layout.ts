/**
 * App-shell geometry. Deliberately NOT in @rift/tokens — a navigation-rail
 * width is a dashboard concept, and the marketing site must not inherit it.
 */
export const SIDEBAR_WIDTH = 268;
export const HEADER_HEIGHT = 72;
export const CONTENT_MAX = 1320;
export const DRAWER_WIDTH = 480;
